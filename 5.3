#include <iostream>
#include <fstream>
#include <string.h>
#include <Windows.h>
using namespace std;

int main()
{

	setlocale(0, "");

	int i = 0;
	int a;
	char separator;
	cout << "Enter the letter that  will separate text   ";
	cin >> separator;
	SetConsoleCP(1251);
	SetConsoleOutputCP(1251);
	char mass[100];
	fstream Fin;
	Fin.open("file.txt");
	Fin.getline(mass, 100);
	Fin.close();
	//cout << "Исходный массив: " << mass << endl;



	for (int i = 0; i < 100; i++)
	{
		if (mass[i] == ' ') {
			mass[i] = separator;
		}
	}
	cout << endl;
	cout << mass;



	return 0;
}
