#include <iostream>
#include <cmath> 
#include <math.h>

using namespace std;
int main()
{
	float s, r, p, m, n;
	cout << "Enter S: " << endl;
	cin >> s;
	cout << "Enter p: " << endl;
	cin >> p;
	cout << "Enter n: " << endl;
	cin >> n;
	r = p / 100;

	m = ((s * r * (pow((1 + r), n))) / ((12 * (pow((1 + r), n) - 11))));
	cout << "m = " << m << endl;
}
