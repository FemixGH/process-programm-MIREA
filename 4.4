#include <iostream>
#include <string>
#include <conio.h>
#include <Windows.h>

using namespace std;

bool a = true;
int num;
string text = "'Past glory' - 1912'";

int main() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);

    while (a == true) {
        num = _getch();

        if (num == 27) {
            exit(0);
        }

        else {

            system("cls");

            cout << endl;

            for (int z = 0; z < text.length(); z++) {
                Sleep(100);
                cout << text[z];
            }




            cout << endl << endl;
            for (int j = 0; j < 8; j++) {
                Sleep(50);
                cout << "-" << " ";
            }
            for (int j = 0; j < 15; j++) {
                Sleep(100);
                cout << (char)127;
            }
            cout << endl;


            for (int i = 1; i <= 48; i++) {
                Sleep(50);
                cout << "*" << " ";
                if (i % 8 == 0) {
                    if (i == 16 || i == 32 || i == 48) {
                        for (int j = 0; j < 15; j++) {
                            Sleep(100);
                            cout << (char)127;
                        }
                    }
                    cout << endl;
                }

            }

            for (int q = 0; q < 3; q++) {
                Sleep(50);
                cout << endl;
                for (int j = 0; j <= 30; j++) {
                    Sleep(100);
                    cout << (char)127;
                }
                cout << endl;
            }

        }

        cout << endl << endl << endl;
        cout << "To restart the program, press any key, to exit - ESC:";
        num = _getch();
        if (num == 27) {
            exit(0);
        }
        else {
            system("cls");
            continue;
        }

    }



}
