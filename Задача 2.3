#include <iostream>
#include <cmath> 
#include <math.h>

using namespace std;
int main()
{
	float x, y, b, result; 
	cout << "enter x: " << endl;
	cin >> x;
	cout << "enter y: " << endl;
	cin >> y;
	cout << "enter b: " << endl;
	cin >> b;

	if (b - y < 1) {
		cout << "Error" << endl;
		exit(0);
	}

	if (b - x < 0) {
		cout << "Error" << endl;
		exit(0);
	}

	result = (log(b - y)) * (sqrt(b - x));
	cout << "z = " << result << endl;
	exit(0);

}
