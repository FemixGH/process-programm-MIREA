#include <iostream>
#include <cmath> 
#include <math.h>

using namespace std;
int main()
{
	double s, m, n, m0, r, p;
	cout << "Enter S: " << endl;
	cin >> s;
	cout << "Enter m: " << endl;
	cin >> m;
	cout << "Enter n: " << endl;
	cin >> n;
	m0 = -100000;
	p = 1;
	/*m = ((s * r * (pow((1 + r), n))) / ((12 * (pow((1 + r), n) - 1))));
	cout << "m = " << m << endl;*/
	do {
		r = p / 100;
		m0 = ((s * r * (pow((1 + r), n))) / ((12 * (pow((1 + r), n) - 1))));
		p = p + 0.00001;
		/*cout << m0 << endl;*/
	} while (m0 < m);
	if (p > 100) {
		cout << "Error" << endl;
	}
	else {
		cout << "procent = " << p << "%" << endl;
	}
}
