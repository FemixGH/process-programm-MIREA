#include <iostream>
#include <cmath> 
#include <math.h>

using namespace std;
int main()
{
	int x;
	cout << "Enter x: = " << endl;
	cin >> x;
	if (x > 0) { 
		cout << "sign (x) = 1"; 
	}else if (x < 0) {
		cout << "sign (x) = -1"; 
	}else {
		cout << "sign (x) = 0"; 
	}
}
